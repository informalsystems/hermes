[package]
name = "no-std-check"
version = "0.1.0"
edition = "2018"

[dependencies]
# ibc = { path = "../modules" }
ibc-proto = { path = "../proto" }
flex-error = { version = "0.4.2", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
